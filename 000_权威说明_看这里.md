# 权威说明(先读这里)

**结果以 `v2_cpp/criteria_v2.md`(权威判据表)为准**;逐轮修正全记录见 `v2_cpp/纠正与重建计划.md`;图见 `v2_cpp/figs_v2/`(**九张**,各 PDF+PNG)。

## 两个数据集(均 κ 无偏,同 seed 20260726,仅初值构造不同)
- **`v2_cpp/instances_floor.csv`**(floorscaled δ=M·κu,M=100):用于 **fig2 可达下限 / C2 / M1–M9(fig9)**。全部实例真迭代到地板,C2=95.57%,e_repr 按精度 u 分层。
- **`v2_cpp/instances_cert.csv`**(kindep δ∈[1e-3,1e-1],κ无关):用于 **fig4 认证价值**(认证收敛到机器精度 / 未认证不收敛的干净分离,认证 573=3.5%)。
- **fig5 warp 调度**用 floorscaled(有难度方差,κ/α/认证键均把浪费 0.63→~0.3);kindep 步数近均匀(0.148)不适合该论证。

## 数据/凭据
- 原始 `*.parquet`/`*.bin` 大文件未入库,可由 `src/gen_instances.py --seed 20260726 --offset_mode {floorscaled|kindep}` 一键重生成(见 README 复现)。
- 连接助手 `_b300.py`(含服务器凭据)已在 `.gitignore` 排除、绝不入库;文档中私有服务器主机名/用户名已脱敏为 `<B300-server>`。

## 陈旧存档(勿用)
仓库根 `results/criteria.txt` 与 `REPORT_实验一_制造根验证.md` 是早期版本,已打"已被取代"横幅,仅作过程留档;以 `criteria_v2.md` 为准。
