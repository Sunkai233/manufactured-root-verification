# 跨卡复现:B300 与 RTX 5090

制造根验证的 GPU 部分(阶 × 精度 吞吐矩阵 solver_matrix + 峰值 FMA 微基准 peak_flops)在两张不同
架构的卡上各跑一遍:数据中心 NVIDIA B300(sm_103)与消费级 RTX 5090(sm_120)。数据集 B_floor.bin 相同,8 步固定迭代。

## 一、精度分层与地板 —— 逐位一致

阶 × 精度矩阵的 relerr 中位数与 p99 在两卡上完全相同(math 与卡无关),见 v2_cpp/matrix_floor.csv(B300)
与 v2_cpp/matrix_floor_5090.csv(5090):

- fp64 relerr_p50: 4.003e-11 / 4.230e-11 / 4.224e-11(Newton/Halley/HH3),两卡逐位相同。
- df32 relerr_p50: 1.823e-09 / 1.535e-09 / 1.440e-09,两卡相同。
- fp32 relerr_p50: 3.155e-05 / 1.106e-05 / 2.520e-05,两卡相同。
- 三种精度按各自单位舍入分层(fp64 ~1e-11 < df32 ~1e-9 < fp32 ~1e-5),det=1 全确定性。

即混合精度可达地板(C5)与阶次-精度权衡是问题与算法的性质,与 GPU 无关。

## 二、吞吐与跨卡 R —— 随卡不同

单卡吞吐(Mroot/s, 8 步):

- fp64  Newton/Halley/HH3:  B300 526 / 460 / 397   |  5090 864 / 755 / 650
- df32  Newton/Halley/HH3:  B300 424 / 406 / 374   |  5090 724 / 663 / 649
- fp32  Newton/Halley/HH3:  B300 42862 / 35511 / 28960 | 5090 24749 / 24693 / 24578

峰值 FMA 微基准(peak_flops):

- B300: fp32 / fp64 峰值比 R ≈ 71(文档值),数据中心卡 fp64 削减较轻。
- 5090: fp32 64803 GFLOP/s、fp64 1655、df32 3299;R = 39.2,即消费卡 fp64 被削减到约 1/39;
  df32 相对 fp64 为 1.99×(纯 FMA),印证 df32 的双单元并行优势。

要点:
- B300 的 fp32 吞吐更高(HBM 带宽 + 数据中心 fp32),5090 的 fp64 与 df32 相对更高(其 R 较小、fp64 削减较轻)。
- df32 约 2× fp64 的 FMA 优势在两卡上都成立;求解器内 df32 较慢仍是其超越函数指令膨胀所致(见正文 C5),非 fp64 的快慢。
- 结论不变:低精度是吞吐杠杆,阶次升高换取每根更贵但更准,精度分层由单位舍入决定而与卡无关。
