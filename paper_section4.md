# 4 制造根验证基准

## 4.1 制造根方法

将偏微分方程数值验证中的制造解方法移植到标量超越方程上。取基础族

g(x) = x e^{a x} + b ln(1 + c x) + d sin(omega x),

其中三项各司其职:指数项提供 Lambert 型的代数与超越耦合,对数项提供位置可控的奇异,正弦项抬高高阶导数的量级。给定精确根 x_star 与目标局部导数 D,构造残差

f(x) = g(x) + s1 x + s0,

由 f(x_star) = 0 确定 s0,由 f'(x_star) = D 确定 s1。由于 k 大于等于 2 时有 f^{(k)} = g^{(k)},源项只改变零阶与一阶信息而不触动高阶结构,于是条件数 kappa 可以独立扫描而不破坏族的其余性质。制造解方法的四条对应关系在此成立:解析解对应精确根,源项对应制造常数,网格加密对应迭代序列,网格收敛阶对应观测迭代阶。作为自检基元,取 b = d = 0,a = 1,c = 1 使 s1 = 0,此时机器方程的真根等于主分支 Lambert W 函数值,以 50 位精度独立核对,最坏相对偏差 1.35e-51(判据 C10)。

## 4.2 双数据集的必要性

初值构造决定该实验能够回答什么问题,因此需要显式说明。早期构造令初值偏移 |x0 - x_star| 正比于 1 除以 gamma,而 log gamma 与 log kappa 的相关系数为 0.99,结果是条件数越大的实例被发生器安排了成比例越靠近根的起跑位置。实测按 kappa 分带,高条件数带内约有百分之九十以上的实例八步迭代一步不动,全体约百分之二十一未移动。这一构造从两处污染结论:其一使误差达标率被并未参与解算的实例抬高,其二在可达下限图的高 kappa 端造出并非物理地板的向下拐折支。故弃用该构造。

代之以两个条件数无偏的数据集,二者使用相同随机种子与相同族参数,仅初值构造不同,从而只改变起点而不改变题目:

其一,floorscaled 数据集,取偏移 |x0 - x_star| = M kappa u |x_star|,M 取 100,即每个实例都起于自身地板上方固定的一百倍,于是所有实例都真正迭代约三步到达各自地板。它回答地板可达性、精度分层与调度这三个问题,用于 4.3 中的 C2、C5 以及阶次与精度全因子和线程束浪费。

其二,kindep 数据集,取偏移 |x0 - x_star| = delta |x_star|,delta 在 1e-3 到 1e-1 之间且与 kappa 无关。它回答认证半径在大偏移下的语义这个问题,用于 4.3 中 C1b 与认证价值的步数分布。

两个数据集的认证数分别为 floorscaled 的 8229(占 50.2 百分比)与 kindep 的 573(占 3.5 百分比)。在两个无偏数据集上,long double 计算的认证结果与 double 完全相等,这一点在 4.4 中作为一个独立结论展开。

## 4.3 逐判据核验

以下逐条给出判据在两个数据集上的取值与适用范围。

C1 观测阶等于形式阶。用 __float128 精度的双偏移单步法测量,即取两个相邻的渐近区偏移各走一步、由误差比消去误差常数后取对数斜率,得 Newton 为 2.000、Halley 为 2.992、Householder-3 为 3.885。收敛阶是迭代映射的性质,与数据集及工作精度无关,两数据集一致。

C1b 固定步数许可。当停机容差取每个实例的可达地板量级,即 epsilon = max(1e-11, 8 u kappa_res),其中 kappa_res 为该点的残差型相对条件数时,认证子集达到 Smale 近似零理论所给上界 k_max = ceil(log2(2 + log2(beta 除以 epsilon))) 的比例为 100 百分比,floorscaled 为 8229 比 8229,kindep 为 573 比 573。若把容差固定在 1e-11,则对满足 u kappa 大于等于 epsilon 的近半数实例而言,该容差落在 fp64 精度地板之下,迭代无法停机。这是终止条件不可达,不是许可公式失效。因此该判据的正确表述为:在停机容差取可达地板量级时,认证子集无一例外满足步数上界。

C2 误差不超过 4 kappa u。在 floorscaled 数据集上,由于所有实例都真正迭代到地板,该比例为 95.57 百分比。按 kappa 分带,kappa 小于 1e6 为 100 百分比,1e6 到 1e8 为 78.5 百分比,此带为八步预算的边际区,kappa 大于等于 1e8 为 100 百分比,此处 4 kappa u 界较宽松。此值的正确表述为,自地板上方一百倍起步、在八步预算内落入 4 kappa u 的实例比例,而非一般意义的精度保证。

C3 认证零误判。在 kindep 数据集的数值可判子集上测量,可判 570 比 573,认证半径 2 beta 内出现第二根的比例为 1.23 百分比。此处 2 beta 是初值到其收敛根的距离上界,并非唯一性球半径,Newton 仍然收敛到目标根,该 1.23 百分比是族本身的多根密度所致,不构成认证错误。认证所用的 alpha 必须以 long double 计算,读取条件数文件缺失时直接报错退出,不静默回退到 gamma。

C4 括号构造有效。所有实例的括号端点异号成立。良态即 kappa 小于 1e4 的实例中,在 x_star 加减 0.2 的窗口内出现至少两根的比例为 76.5 百分比,至少三根为 9.9 百分比,测量使用 long double 网格且关闭浮点乘加收缩。此量只用到 x_star 与族参数、不涉及初值,故与数据集无关,两数据集的分带计数逐个相同。

C5 混合精度可达下限。真源为 floorscaled 数据集。认证子集的可达相对误差中位数为 fp64 的 1.11e-13、df32 的 5.31e-12、fp32 的 1.44e-6,三者按各自单位舍入分层,见 Figure 4.2。kindep 数据集上以 x_star 为参照的可达相对误差不是有效度量:其 573 个认证实例中有 119 个即 20.8 百分比的 fp64 误差大于 1e-6,且三种精度的误差比恒为 1.000,这是三种精度共同收敛到邻根的指纹,而非精度地板。其成因是 delta 取到 1e-1 叠加 C4 所示的 76.5 百分比多根,初值落入邻根吸引域为必然事件。混合精度层级另有机器级证据:反汇编固定步核可见,fp64 每根 104 条双精度乘加,df32 为 1267 条单精度乘加,fp32 为 111 条单精度乘加加 26 条硬件超越函数指令。峰值微基准显示两张 Blackwell 卡的双精度吞吐均被大幅削减,B300 的单精度与双精度吞吐比为 71.4,RTX5090 为 40.6,df32 的纯乘加吞吐反而高于原生双精度,分别为 3.8 倍与 2.0 倍,求解器内 df32 较慢的原因是其超越函数的指令膨胀而非双精度的快慢。阶次与精度的全因子对照见 Figure 4.9,八步固定、饱和吞吐下,吞吐为 fp64 的 526、460、397,df32 的 424、406、374,fp32 的 42862、35511、28960,单位为百万根每秒,阶次升高则每步更贵故吞吐下降,单精度远高于其余两者。

C6 梯度精度。将求解器输出根关于方程五个参数的梯度以前向对偶数自动微分与隐函数定理闭式两种方式在收敛点逐实例对照,良态即 kappa 小于 1e2 的相对误差中位数为 2e-15,并随 kappa u 增长,无符号翻转,见 Figure 4.3。

C7 梯度连续与可微。以三条独立路径互证:展开迭代的前向对偶数自动微分,隐函数定理闭式,以及穿过整个求解器的自适应步长中心差分。两条解析路径全程一致,中心差分在 kappa 小于 1e4 达到差分地板,超出后按 1 除以 f 的一阶导放大而退化,属问题本身的条件性而非实现缺陷。

C8 确定性。计时核三次运行逐位一致,N 为 16384 与饱和的 4.19M 两种规模均如此。

C9 加速比与能耗。同一算法下,GPU 满卡相对 CPU 单核约 109 倍,相对 128 线程约 1.9 倍。单核 Brent 因每根所需评估次数少而吞吐高于单核 Householder-3,其价值不在单点吞吐而在批量确定性与可微,按设计边界表述。能耗在扣除空载并使用配套饱和吞吐后,fp64 为 0.2592,df32 为 0.6678,fp32 为 0.0200,单位为焦耳每百万根。线程束浪费在 floorscaled 数据集上,随机排布为 0.632,按先验条件数、alpha、认证标志排序分别降到 0.268、0.296、0.317,固定步核由构造归零,见 Figure 4.5。kindep 数据集因仅 3.5 百分比收敛、其余步数几乎全部顶到步数上限,六个调度键被压平,属封顶伪影而非调度结论,故不用于该论证。

C10 自检基元。见 4.1,最坏相对偏差 1.35e-51。

## 4.4 两个独立小结论

其一,可迭代到达地板的条件数上界。牛顿近似零的收敛盆地半径约为 alpha0 除以 gamma,alpha0 为认证阈值 0.157671。当某根的条件地板 kappa u 超过该盆地半径时,任何有界初值都无法通过迭代到达地板,只能在起点即置于地板处。本族 gamma 与 kappa 相关 0.99,以 1 除以 kappa 作量级估计,得可迭代展示地板的条件数上界约为 1 除以 u 的平方根,在 fp64 下约 1.3e8,该上界直接由精度 u 决定。早期正比于 1 除以 gamma 的构造正是靠把初值预置于地板处掩盖了这一极限,条件数无偏的初值将其暴露,见 Figure 4.2 中的竖线。

其二,β 等于零相消的条件性。double 下 beta 等于 f(x0) 除以 f 一阶导的绝对值,在初值近根时因灾难性相消而舍入为零,从而使 alpha 为零并把极病态实例误判为认证。此现象并非一般的 double 缺陷,而只在初值距根不超过 kappa u 量级时出现。反证在于:floorscaled 数据集的初值距根为一百倍 kappa u,此时 long double 的认证数等于 double 的认证数,均为 8229,且 beta 等于零的计数为零,double 与 long double 的判定不再有任何差异。因此该相消被刻画为初值距根不超过 kappa u 这一充分条件下的产物,而非精度本身的普遍缺陷。
