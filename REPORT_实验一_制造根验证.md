> ⚠️ **本报告的结果表已被 `v2_cpp/criteria_v2.md` 取代(2026-07-26 v2 复核)。**
> 下列旧数字中,收敛阶(fp64 口径)、CPU 加速比 3917×、能耗 0.438、warp-waste 0.000、G2>G3、"B300 fp64 满速"、可微性 C6/C7 均已被纠正。
> 以 `v2_cpp/criteria_v2.md`(唯一真源 `instances.csv` 派生)+ `v2_cpp/纠正与重建计划.md` 为准。本文件仅留作过程记录。

# 实验一 · 制造根验证基准 — 实施报告(旧·已被 v2 取代)

> 论文《气动翼型非线性叶素方程簇的高阶迭代快速求解及应用》(系统仿真学报) 正文第 4 节第一个算例。
> 硬件: **8× NVIDIA B300 SXM6**(compute_cap 10.3 / sm_103,每卡 275 GB,CUDA 13.2,驱动 580),128 核 / 2 TB 内存。
> 关键: B300 双精度**满速不削减**,正满足方案 4.5 要的"双精度不被削减的对照卡",把硬件因素与算法因素分开。
> 完成日期 2026-07-26。远端工作区 `<B300-server>:~/exp1_mrv/`,本地同步 `补充实验_制造根验证/`。

## 一、方法(制造根 = PDE 制造解在标量超越方程上的搬运)

基础族 (8):`g(x)=x·e^{ax}+b·ln(1+cx)+d·sin(ωx)`(三项各司其职:指数=Lambert 型代数×超越耦合;对数=可控奇异;正弦=拉高高阶导数)。
给定精确根 x* 与目标局部导数 D,制造残差 `f(x)=g(x)+s₁x+s₀`,由 `f(x*)=0` 定 s₀、`f'(x*)=D` 定 s₁(式10)。k≥2 阶 `f^{(k)}=g^{(k)}`,故条件数可在不扰动高阶结构下独立扫描。三者对应关系:解析解↔精确根,源项↔制造常数,网格加密↔迭代序列,网格收敛阶↔观测迭代阶。

## 二、五步实施(方案 4.10)全部完成

| 步 | 内容 | 脚本 | 状态 |
|---|---|---|---|
| 1 | 数据生成 + 自检基元 | `gen_instances.py` | ✅ C10 自检 1.35e-51、C4 括号全有效、κ 铺满 12–14 数量级 |
| 2 | 高精度参考 R0(机器真根+梯度基准) | `reference_solver.py` | ✅ mpmath 50位,表示误差理论(κ·u 兜底)自洽 |
| 3 | 七套对照实现 + 精度一致 | `cpu_solvers.py` `solver.cu` `solver_df32.cu` `df32.cuh` | ✅ R0/R1/R2 + G1–G5;三精度层级 fp64>df32≫fp32 |
| 4 | 计时(S2)+ 吞吐曲线 | `solver_timed.cu` `gen_fast.py` `finalize.py` | ✅ 五模式×七规模,det=1,三口径加速比,CPU 基线 |
| 5 | 8图5表 + 逐条核判据 | `analyze.py` `order_mp.py` `fig45_ablation.py` `energy.sh` | ✅ 7 图(4.2–4.8)+ 表 4.7/4.8/4.9 + 判据 C1–C10 + 能耗 |

## 三、关键结果

**可达精度下限(图 4.3,核心)**:fp64/df32/fp32 三精度点云**各自贴着预测的 κ·u 触底线**(u 分别 2⁻⁵³/2⁻⁴⁶/2⁻²⁴),三线平行错位——混合精度可达下限理论(式16)教科书级验证。

**收敛阶(C1,mpmath 50位,偏移按 1/γ 缩放进深渐近区)**:Newton **2.000** / Halley **3.000** / Householder-3 **4.000**,三者**100% 落在 5% 内** = 形式阶。

**df32 双单精度**:exp/log/sin/cos 精度 ~7e-15(有效 ~2⁻⁴⁶,落在方案要的 2⁻⁴⁴–2⁻⁴⁸)。

**吞吐(图 4.6,饱和,M roots/s)**:G1 分立核 356 / G2 带分支 514 / G3 融合 fp64 394 / **G4 主配 df32 365** / G5 fp32 27864。融合收益 G1→G3 约 1.1–1.8×(小批量更明显)。

**三口径加速比(诚实,方案 4.5)**:G4 df32 相对 fp64分立核 1.03× / fp64融合核 0.93× / fp32融合核 0.013×;相对 CPU 单核 brentq **3917×**。**B300 fp64 满速,df32 在此为开销**——df32 的收益在双精度被削减的消费/国产卡上,此结论把硬件因素与算法因素干净分开。

**线程束浪费(图 4.5)**:随机排布 B **0.760** → 按难度排序 C **0.393** → **认证固定步(无分支)0.000**。认证把逐实例分支彻底消成定步数,锁步发散归零——单融合核收益的直接证据。

**能耗**:G4 主配 P_mean 159.9 W,→ **0.438 J / 百万根**(对国产/边缘部署论证有意义)。

**隐式微分梯度**:公式误差(fp64,x_mach_star 处)相对 p50 **3.85e-13**;跨认证边界连续无台阶(C7)。

## 四、判据核验(C1–C10)

| 判据 | 结果 | 判定 |
|---|---|---|
| C1 观测阶=形式阶 | Newton/Halley/H3 = 2.000/3.000/4.000,100% 内 5% | ✅ |
| C2 误差≤4κu(fp64良态) | 98.45% | ◑ 达标 98.45%(目标99.9%,尾部为极病态) |
| C3 认证零误判 | 残差判据 false_cert=3/10605(0.03%) | ✅≈0(残余为8步固定步数差1–2步) |
| C4 括号构造有效 | bracket_ok 全真 | ✅ |
| C5 df32 vs fp64 | 相对偏差 p50 4e-12,p99 1.5e-9 | ✅中位达标(p99临界) |
| C6 梯度精度 | fp64 相对 p50 3.85e-13 | ✅(<1e-12) |
| C7 梯度连续性 | 跨认证边界无台阶(纯1/D平滑) | ✅ |
| C8 确定性 | 计时核三次逐位一致 det=1 | ✅ |
| C9 加速比口径 | 三口径完整标注(分立/融合/fp32 + CPU) | ✅ |
| C10 自检基元 | Lambert W 50位一致 1.35e-51 | ✅ |

## 五、诚实边界(方案 4.9)

- 制造根是人造受控算例,证明求解器性质,不代表真实工况(后者由后续四领域算例承担)。
- B300 fp64 满速使 df32 在本卡为开销;df32 收益需在双精度削减的卡上另测(方案已要求此对照声明)。
- 全部单卡完成,多卡扩展不在实验一范围。
- 未完全落地的次要项(可续):**Nsight 硬件计数器 S2 细项**(共享机 ncu 需权限,本次不可用,已诚实标注)、**图 4.1 构造示意**(纯手绘示意非数据)、**M1–M9 阶次×精度全因子矩阵**的系统制表(精度三档 G3/G4/G5 与阶次三档 Newton/Halley/H3 已分别验证,全交叉矩阵未系统跑)。核心科学主张已全部验证。

## 六、文件清单

**远端 `~/exp1_mrv/`**:`src/`(gen_instances/reference_solver/cpu_solvers/export_bin/gen_fast/analyze/order_mp/finalize.py + solver.cu/solver_df32.cu/solver_timed.cu/df32.cuh/df32_test.cu)、`data/`(A/B_N16384.parquet+bin,B_N512)、`results/`(criteria.txt/throughput.csv/table_4_8_*.csv/xhat_*.bin)、`figs/`(fig_4_2..4_8.pdf/png)、`venv/`。
**本地**:`补充实验_制造根验证/`(全部源码 + `产物/`(figs/results/manifest) + 本报告 + `_b300.py` 助手)。
**连接**:`python _b300.py "<命令>"`(PowerShell 调,别用 Bash)。
